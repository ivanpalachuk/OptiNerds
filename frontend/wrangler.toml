# Cloudflare Workers — deploy frontend estático desde monorepo
# Wrangler debe ejecutarse desde esta carpeta (frontend/), no desde la raíz
name = "optinerds"
compatibility_date = "2026-04-13"

[assets]
directory = "./dist"
